import type { FailingResource } from "./derive";

export interface FailingResourcesAccordionProps {
  failing: FailingResource[];
  releaseMessage?: string;
}

// Temporary stub — replaced in the failing-resources-accordion task (Task 12).
export function FailingResourcesAccordion(_props: FailingResourcesAccordionProps) {
  return null;
}
