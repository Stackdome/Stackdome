# Logs
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static

# Local visual-review scratch harnesses (Playwright against a running Storybook
# or `dev:mock`). Named patterns, not exact files — every design session writes
# another one-off prober and four of them had already escaped into `git status`.
.shot*.mjs
.measure*.mjs
.probe*.mjs
.preview-shot.mjs
